import styled from 'styled-components'

export const LayoutContainer = styled.div`
  max-width: 90rem;
  background: ${props => props.theme['gray']['gray-100']};
  border-radius: 8px;
  display: flex;
  margin: auto;
`
